
import React from "react";

export default function App() {
  return (
    <div style={{padding: 24}}>
      <h1>Heart Disease Prediction (React Frontend - Optional)</h1>
      <p>Connect this UI to Flask via REST endpoints (e.g., /api/predict).</p>
    </div>
  );
}
